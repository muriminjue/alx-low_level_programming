#include <stdio.h>

/**
 * main - printsfiles name
 * @argc: number of arguments
 * @argv: array of argument pointers
 * Return: 0 id succes
 */
int main(int argc, char *argv[]) 
{
printf("%s\n", argv[0]);
return (0);
}
