#include <stdio.h>

/**
 * main - app entry
 * print all alphabets 
 *
 * Return: 0 (success)
 */
int main(void)
{
int num;

for (num = 0; num < 10; num++)
{
printf("%d", num);
}
printf("\n");
return (0);
}
