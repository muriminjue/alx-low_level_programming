#include <stdio.h>

/**
 * main - app entry
 * uses puts to print string
 *
 * Return: 0 (successful)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
