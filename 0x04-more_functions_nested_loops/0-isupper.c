#include "main.h"

/**
 * _isupper - checks if uppercase
 * @c: The character to check
 * Return: On success uppercase 1 else 0
 */
int _isupper(int c)
{
if(c >= 'A' && c <= 'Z' )
{
return (1);
}
else
{
return (0);
}
}
